{
  "//": "Test-only tsconfig. IDENTICAL to tsconfig.json except `jsx`, which Next sets to `preserve` because ITS bundler owns the transform. Node's test runner has no bundler, so tests compile JSX with the automatic runtime instead. Nothing else differs — a test must typecheck against the same rules the app does.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": ["node"]
  },
  "include": ["next-env.d.ts", "src/**/*.ts", "src/**/*.tsx", "test/**/*.ts", "test/**/*.tsx"]
}
