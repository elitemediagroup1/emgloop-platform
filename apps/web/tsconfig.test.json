{
  "//": "Test-only tsconfig. Differs from tsconfig.json in exactly two ways: `jsx` compiles with the automatic runtime because Node has no bundler, and `server-only` maps to a local stub because that module is provided by Next rather than installed. Neither affects the app build.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "jsx": "react-jsx",
    "types": [
      "node"
    ],
    "paths": {
      "@/*": [
        "./src/*"
      ],
      "server-only": [
        "./test/_server-only.ts"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "src/**/*.ts",
    "src/**/*.tsx",
    "test/**/*.ts",
    "test/**/*.tsx"
  ]
}
