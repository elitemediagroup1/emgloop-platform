{
  "name": "@emgloop/infra-connections",
  "version": "0.1.0",
  "private": true,
  "description": "The connections worker environment on AWS (Teams/Telegram durable worker). A separate deployable, not an npm workspace, so the web build never installs it. Staging only.",
  "scripts": {
    "synth": "CONNECTIONS_SYNTH_OFFLINE=1 cdk synth --quiet --no-notices",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "engines": { "node": ">=22.0.0" },
  "dependencies": {
    "aws-cdk-lib": "2.269.0",
    "constructs": "10.8.1"
  },
  "devDependencies": {
    "@types/node": "22.20.3",
    "aws-cdk": "2.1142.0",
    "tsx": "4.23.13",
    "typescript": "5.9.3"
  }
}
